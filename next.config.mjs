
    /** @type {import('next').NextConfig} */
    const nextConfig = {
      reactStrictMode: true,
      images: {
        domains: ['example.com'], // Substitua pelo domínio real de imagens
      },
    };

    module.exports = nextConfig;
    